(executable
  (name main)
  (libraries iter containers gen imagelib tsdl owl-base)
  (preprocess (pps ppx_deriving.show ppx_deriving.enum))
  (modes exe))


(env
  (dev
    (flags (:standard -warn-error -A))))
